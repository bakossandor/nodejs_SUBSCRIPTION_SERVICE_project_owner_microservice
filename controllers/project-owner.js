async function getProjectOwner (req, res, next) {
  res.send({'developerMessage': 'under construction'})
}

async function patchProjectOwner (req, res, next) {
  res.send({'developerMessage': 'under construction'})
}

module.exports = {
  getProjectOwner,
  patchProjectOwner,
}